{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "version": 2,
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @cyberlisans/api exec tsc --noEmit && echo 'Typecheck-only build; @vercel/node compiles entrypoint via esbuild'",
  "builds": [{ "src": "src/vercel-entry.ts", "use": "@vercel/node" }],
  "routes": [{ "src": "/(.*)", "dest": "src/vercel-entry.ts" }]
}
