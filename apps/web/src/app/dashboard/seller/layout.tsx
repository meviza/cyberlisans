/**
 * Seller routes inherit DashboardAppShell (path-aware variant=seller).
 * Nested shell removed to avoid double topbar/sidebar.
 */
export default function SellerLayout({ children }: { children: React.ReactNode }) {
  return children;
}
