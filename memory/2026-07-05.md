# 2026-07-05 — M3 Kapanış + Döküman Sistemi + M4 Planı

## Bugün yapılan işler

### M3 Escrow akışı tamamlandı (production deploy M3.1 blocker)

- ✅ DB: `0005_escrow_auto_release_and_payout_window` migration uygulandı
  - `escrow_transactions.payoutEligibleAt` kolonu eklendi
  - `seller_payouts` tablosuna commission/gross/currency kolonları eklendi
  - 3 PL/pgSQL fonksiyonu: `auto_release_escrow()`, `release_escrow()`, `refund_escrow()`
  - 7 RLS policy eklendi (commissions, seller_payouts, disputes, dispute_messages)
- ✅ Backend: 5 use-case yazıldı
  - `create-escrow.ts` (102 satır) — escrow oluşturur, 7 gün releaseAt hesaplar
  - `release-escrow.ts` (48 satır) — RPC ile release
  - `request-payout.ts` (99 satır) — satıcı payout talebi (min 50 TRY, balance check)
  - `create-dispute.ts` (85 satır) — müşteri itiraz açar
  - `resolve-dispute.ts` (106 satır) — admin çözer (REFUND/RELEASE/PARTIAL)
- ✅ Backend: 4 API route + 3 repository + 2 port + 1 entity + 1 error sınıfları
  - `escrow.ts` (98 satır), `payouts.ts` (77 satır), `disputes.ts` (100 satır)
  - `admin/escrow.ts` (128 satır) + `escrow.schema.ts` (25 satır)
- ✅ Frontend: 7 page + 14 component + sidebar güncelleme
  - Customer: checkout, orders list, orders/[id]
  - Seller: payouts
  - Admin: disputes, disputes/[id], escrow
- ✅ Local'de tüm endpoint'ler Newman + manual curl ile test edildi
- ❌ Vercel production: alias drift sorunu, M3 endpoint'leri Vercel'de 404
  - Blocker olarak M3.1'e kaydedildi

### Araç entegrasyonları (paralel 4 ajan)

- ✅ **Postman**: 21 endpoint, 6 klasör, 105/105 Newman testi geçti, GitHub Actions workflow hazır
- ✅ **Trigger.dev**: version 20260704.1 deploy edildi, `proj_sibrytqjplnlnkvxwfve`
  - `src/trigger/release-escrow.ts` — her gün 03:00 Istanbul saatinde auto-release
  - HMAC service-only endpoint: `/api/internal/auto-release`
- ⚠️ **Sentry**: DSN + env eklendi ama `withSentryConfig` build sırasında source map upload deniyor, build kırılıyor
  - `next.config.mjs`'den Sentry wrapper kaldırıldı, geçici olarak devre dışı
- ⚠️ **Vercel env**: 3 env eklendi (SENTRY_DSN, NEXT_PUBLIC_SENTRY_DSN, INTERNAL_SERVICE_SECRET)
  - ama Vercel CLI alias drift yapıyor, `cyberlisans.vercel.app` farklı projeye atanmış
  - Yeni subdomain `cyberlisans-mp.vercel.app` eklendi, login ✅ çalışıyor, M3 endpoint'leri hâlâ ❌

### Döküman sistemi kuruldu

- ✅ `memory/` klasörü oluşturuldu (günlük ham log)
- ✅ `docs/STATUS.md` — tek sayfa özet, her oturum başında okunacak
- ✅ `docs/CHANGELOG.md` — milestone bazlı semver changelog
- ✅ `docs/CONTEXT.md` — AI agent'ları için tam context (kim, ne, nasıl, neden)
- ✅ `docs/RUNBOOK.md` — operasyon rehberi (deploy, rollback, debug, monitoring)
- ✅ `docs/ROADMAP.md` güncellendi — M3 → M4 geçişi
- ✅ `docs/ARCHITECTURE.md` güncellendi — supabase-js katmanı + Sentry

### Kararlar

1. **Vercel/Sentry M4 blocker'ı değil, M3.1 backlog'a alındı.** Local geliştirme + Postman + Trigger + Supabase yeterli.
2. **M4 paralel 4 ajan başlatılacak**: backend use-case, seller UI, admin UI, DB seed + Postman.
3. **Her milestone sonunda `STATUS.md` + `CHANGELOG.md` güncellenecek.**

## Dersler (bugünden)

1. **Vercel CLI alias komutu broken** — `vercel alias set <domain> <deployment>` sürekli "deployment not found" diyor, subdomain workaround ile çözüldü.
2. **Sentry Next.js wrapper build'i kırıyor** — `withSentryConfig` source map upload sırasında auth token istiyor, eksikse build fail. Token olmadan kullanmak için `silent: true` + auth token stub gerekir.
3. **apps/api Vercel bundling** — `apps/api/src/app.ts` tüm route'ları içeriyor ama Vercel'in eski deployment cache'i yeni route'ları görmüyor. `--force` flag ile çözüldü.
4. **apps/api env loading** — `process.env` Vercel'de doğru yükleniyor ama `INTERNAL_SERVICE_SECRET` eşleşmedi — sebep muhtemelen CLI'nın secret encoding'i bozması.

## Aktif TODO (sonraki oturum)

- [ ] **M4 paralel 4 ajan başlat** (aşağıda)
- [ ] M3.1: Vercel alias drift'i çöz (subdomain alias ataması manuel)
- [ ] M3.1: Sentry clean install (paket kaldır, pnpm install, wrapper'sız deploy)
- [ ] M4 sonunda: Newman full test + commit + tag v4.0-products
- [ ] Backlog: payments gerçek entegrasyonu (PayTR, Papara), email verification, 2FA aktifleştir

## Commit/Tag özeti (bugün)

- Tag: **v3.0-escrow** (M3 için)
- Commit: milestone raporu + 5 döküman + M3 backend/frontend kodu
