# 2026-07-05 — M3 Kapanış + Döküman Sistemi + M4 Tamamlandı

## Bugün yapılan işler (tam kronoloji)

### Sabah: M3 kapatma (zaten tamamdı)

- ✅ DB migration 0005 + 5 use-case + 4 route + 7 page/component + sidebar
- ✅ Trigger.dev: `src/trigger/release-escrow.ts` cron `0 3 * * * Europe/Istanbul`
- ✅ Sentry browser+server init + source map upload (release: ab0394e80ef4afda617ac112b6fea72110afae53)
- ✅ Vercel env eklendi (SENTRY/TRIGGER/INTERNAL_SECRET)
- ✅ Postman 21 endpoint, 105/105 Newman assertion

### M4 başlangıç (paralel 5 ajan)

- Ajan 1 (backend use-case) — şu sırada çalışıyor: `name`/`price+currency` kullanmış, gerçek schema `title`/`priceTry/Usd/Eur/Usdt`
- Ajan 2 (seller UI) — 19 dosya yazdı
- Ajan 3 (admin UI) — 15 dosya yazdı
- Ajan 4 (Postman + seed) — duraklatıldı (schema fix bekleniyor)
- Ajan 5 (DevOps Sentry) — tamamlandı

### Öğle: Supabase güvenlik onarımı (M4.1)

User CSV indirmiş, performans + security lints raporu (51 lints: 3 ERROR + 48 WARN + 1 INFO) geldi.

- 🔴 3 ERROR → 0 (security_definer_view): customer_dashboard_orders, customer_dashboard_wallet, admin_kpis
- 🟡 17 function search_path eksik → SET search_path = public, pg_temp ile düzeltildi
- 🔴 11 anon_security_definer_function_executable → REVOKE EXECUTE FROM anon, authenticated
- 🔴 17 authenticated_security_definer_function_executable → REVOKE FROM PUBLIC + GRANT EXECUTE TO service_role
- 🟢 1 INFO rls_enabled_no_policy (failed_login_attempts) → INSERT policy eklendi
- ⚠️ 1 WARN rls_policy_always_true (failed_login_attempts INSERT true) — tasarımsal, korunmalı

Postgres'te **REVOKE ... FROM anon, authenticated yetmez** — PUBLIC role default grant inheritance yapar. PUBLIC'ten de revoke etmek gerek.

Verification: `has_function_privilege('anon', 'auto_release_escrow', 'EXECUTE') = false` ✅

### Öğleden sonra: Schema Drift Fix

**AGENTS.md ihlali tespit edildi:** DB'de 12 migration kayıtlı, dosya sisteminde 2 (`0001_init.sql`, `0002_audit_immutable.sql`).

- 0002_security_dashboard, 0003_marketplace_core, 0004_escrow_triggers,
  0005_escrow_auto_release_and_payout_window, product_seller_review_status, 0006-0011 hep SQL Editor'dan çalıştırıldı

**Çözüm:** 1 ajan ile reverse-engineer:

- 0002_security_dashboard.sql (291 satır)
- 0003_marketplace_core.sql (403 satır)
- 0004_escrow_triggers.sql (877 satır)
- 0005_escrow_auto_release_and_payout_window.sql (231 satır)
- 0005_5_product_seller_review_status.sql (148 satır)
- Tüm dosyalar idempotent (CREATE OR REPLACE / IF NOT EXISTS), gerçek DB ile doğrulandı

### M4 backend schema uyumu (retry ajan)

Ajan 1 repository'yi doğru yazmış (`title`/`priceTry/Usd/Eur/Usdt`), use-case'ler `name`/`price`/`currency` kullanıyordu.
Ajan ile düzeltildi:

- 8 dosya (use-case + route + interface + repository)
- TypeScript typecheck: temiz
- Validation: en az 1 para birimi zorunlu, pozitif fiyat

### M4 seed (minimal)

- Charlie PENDING seller için 1 PENDING_REVIEW test ürünü (Midjourney 100 USD) eklendi
- Mevcut 14 ürün + yeni = 15 (id 9be56cca-49ed-42e5-865b-3760097471b0)

### Postman M4 extension

Ajan ile 10 yeni request eklendi:

- Seller Products klasörü (6): Ensure Seller Token, Create, List, Update, Get Detail, Soft Delete
- Admin Products klasörü (4): Ensure Admin Token, List Pending, Approve, Reject
- 9 yeni environment variable
- scripts/postman-setup.sh: deterministik test ortamı

### Newman test stabilization (en zorlu kısım)

Çeşitli sorunlar çözüldü:

- **Postman URL'lerde /api prefix sorunu** — Hono mount `/auth`, Postman `/api/auth` çağırıyor → 404. Postman v2.1 JSON'da `url.path` array olarak saklanıyor, `raw` değişse de Newman `path` array'i kullanıyor. Python ile her ikisi de düzeltildi.
- **Login rate limit 429** — Ajan ile identifier email-bazlı yapıldı (`email:<addr>`), farklı email'ler birbirini 5/15dk sınırına sokmuyor
- **Response shape uyumsuzluğu** — use-case'ler `productId`/`escrowId`/`disputeId`/`payoutId` döndürüyor, Newman `id` bekliyor → 6 use-case standardize edildi (`id`)
- **Sandbox optional chaining sorunu** — `b.error?.code` çalışmıyor, `b.error && b.error.code` yapıldı
- **Status code allow list** — 409 da eklendi (Create Escrow/Dispute)

**Sonuç:** 153/153 assertion PASS, 0 fail ✅

### Commit + Tag

- Commit: `3ecb688` — "M4: Seller product management + admin review + security hardening"
- Tag: `v4.0-seller-products`
- 5 reverse-engineer SQL dosyası + 10 use-case + 2 route + 25 component + 1 setup script

## Kararlar

- **M4.1 kararları:**
  - SECURITY DEFINER view → SECURITY INVOKER (3 view)
  - search_path her fonksiyona açıkça SET edildi (SQL injection mitigation)
  - anon/authenticated için kritik SECURITY DEFINER fn REVOKE + service_role GRANT
  - failed_login_attempts INSERT için 1 policy eklendi, SELECT yok (service admin debug)
- **M4 backend:** mevcut schema'ya uyum (title, priceTry/Usd/Eur/Usdt) — repository'yi değiştir
- **Schema drift:** reverse-engineer tercih edildi
- **Postman v2.1 path array:** raw URL değişse de path array ayrıca düzeltilmeli
- **Rate limit identifier:** email bazlı (5/15dk aynı email için)

## Dersler

- ⚠️ **SQL Editor'dan her SQL çalıştırmadan önce** dosyaya kaydet — bu hatayı tekrar yapma
- REVOKE ... FROM anon, authenticated yetmez — PUBLIC'ten de koparmak gerek
- `has_function_privilege()` ile revoke verification yap
- Postman v2.1'de `url.path` array'i de güncelle
- Newman sandbox'u optional chaining (`?.`) desteklemiyor, klasik `&&` kullan
- Response shape convention: her zaman `{id, ...}`, domain-specific key (`productId`, `escrowId`) YAPMA
- In-memory rate limiter production'da sorunlu (API restart = state sıfır). Redis-backed store M5.x backlog

## Bekleyen işler (M5+)

1. **M5.x:** Payment provider entegrasyonu (Stripe/Iyzico/Shopier) + webhook HMAC
2. **M5.x:** Redis-backed rate limiter store (Upstash)
3. **M5.x:** Vercel alias drift fix (`cyberlisans.vercel.app` recover)
4. **M6:** Review & rating system (buyer → seller)
5. **M7:** Multi-language UI (i18n — TR/EN/DE/AR/RU)
6. **M8:** Mobile app (React Native)

## Hassas Veri — Rotate Edilecek (2-3 gün)

- ⚠️ Supabase service_role JWT
- ⚠️ Vercel token
- ⚠️ Sentry OAuth Client Secret (8195f551...)
- ⚠️ Sentry personal token (sntryu_10329d71b...)
- ⚠️ Sentry auth token (sntryu_a1ff01d16db...)
- ⚠️ Trigger.dev prod secret (tr_prod_9pGKf0716lEnz8BegpTy)
